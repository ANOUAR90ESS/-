# خطة تطوير المنصة — أفكار مشاريع سهلة ومربحة

> وثيقة تخطيط للميزات القادمة، مبنية على تحليل الوضع الحالي للكود (React 19 + Vite + Tailwind 4 + Express + Gemini).

---

## 1. أين نحن الآن؟

**الموجود فعلياً:**

| المكوّن | الحالة |
|---|---|
| `src/data/projectsData.ts` | 12 فكرة مشروع ببيانات غنية (تكلفة، ربح، خطوات، تسويق، اقتصاديات الوحدة) |
| `App.tsx` | بحث + تصفية بـ 7 تصنيفات + ترتيب بـ 3 معايير |
| `ProjectCard` / `ProjectDetailModal` | عرض البطاقات والتفاصيل الكاملة |
| `ProfitCalculator` | حاسبة ربح بسيطة (سعر، تكلفة، عدد وحدات، مصاريف ثابتة) |
| `AiPlanGenerator` | دراسة جدوى بالذكاء الاصطناعي عبر `/api/generate-plan` |
| `IdeaMatchmakerQuiz` | اختبار من 4 أسئلة لاقتراح المشروع الأنسب |
| `FavoritesDrawer` | مفضلة محفوظة في `localStorage` |
| `server.ts` | Gemini مع 3 نماذج احتياطية + خطة ذكية محلية عند فشل الـ API |

**الفجوات الأساسية (وهي مصدر معظم الأفكار أدناه):**

1. **المحتوى ينتهي بسرعة** — 12 فكرة فقط، والمستخدم يستهلكها في جلسة واحدة ولا يعود.
2. **لا يوجد "بعد الاختيار"** — المستخدم يختار مشروعاً ثم... لا شيء. لا تنفيذ، لا متابعة، لا عودة.
3. **لا يوجد رابط لكل مشروع** — لا Routing، فلا مشاركة ولا أرشفة في محركات البحث (خسارة كاملة للزيارات المجانية).
4. **الأرقام بالدولار فقط** — والجمهور عربي بعملات محلية متباينة تماماً.
5. **الخطة المولّدة تضيع** — نسخ نصي فقط، لا حفظ ولا PDF ولا رجوع إليها.
6. **لا توجد بيانات عن الاستخدام** — لا نعرف أي فكرة تهم الناس فعلاً.

---

## 2. الميزات المقترحة

### المرحلة الأولى — أساسيات مفقودة (أثر عالٍ / جهد منخفض)

#### 1.1 روابط مباشرة لكل مشروع + تحسين الظهور في البحث
لا يوجد اليوم أي Routing. إضافة مسار `/project/:id` يعني أن كل فكرة تصبح صفحة قابلة للمشاركة على واتساب وفيسبوك، وقابلة للأرشفة في جوجل.

- إضافة `react-router-dom` أو Routing بسيط عبر `history.pushState`.
- مشاركة الحالة في الرابط: `?category=digital&sort=profit&q=تصميم`.
- توليد `sitemap.xml` + وسوم `og:` ديناميكية لكل مشروع (تُحقن من الخادم في `index.html`).
- زر "مشاركة" يستخدم `navigator.share` على الهاتف ونسخ الرابط على سطح المكتب.

**لماذا؟** هذه أرخص قناة نمو موجودة. بدونها كل الزيارات يجب شراؤها.

#### 1.2 دعم العملات المحلية
كل الأرقام مكتوبة بالدولار (`capitalRange`, `monthlyProfitRange`, الحاسبة). المستخدم في المغرب أو مصر أو السعودية لا يقيس بالدولار.

- `CurrencyContext` مع قائمة عملات (SAR, AED, EGP, MAD, DZD, TND, KWD, QAR, JOD, IQD, USD).
- أسعار صرف ثابتة قابلة للتحديث في `src/data/currencies.ts` (لا حاجة لـ API خارجي في البداية).
- دالة `formatMoney(amount, currency)` تُستخدم في كل مكان بدل `$` المكتوبة يدوياً.
- حفظ اختيار المستخدم في `localStorage` + محدد العملة في الـ `Header`.

#### 1.3 تصدير دراسة الجدوى (PDF / طباعة)
حالياً الخطة تُنسخ كنص خام فقط، فتفقد قيمتها لحظة إغلاق النافذة.

- ورقة أنماط `@media print` مخصصة + زر "تحميل PDF" يستدعي `window.print()`.
- هيدر احترافي: اسم المشروع، التاريخ، اسم المنصة.
- زر "إرسال على واتساب" بملخص مختصر (`wa.me/?text=`).

#### 1.4 مكتبة خططي المحفوظة
- حفظ كل خطة مولّدة في `localStorage` (مع الحقول المدخلة، ليمكن إعادة التوليد).
- درج جانبي "خططي" شبيه بـ `FavoritesDrawer` لعرضها وحذفها ومقارنتها.
- تصدير/استيراد JSON لنقل البيانات بين الأجهزة.

#### 1.5 إصلاحات سريعة في الكود القائم
- `sortBy === 'speed'` يستخدم `localeCompare` على نص عربي حر ("خلال 3 أيام" مقابل "أسبوعان") فترتيبه غير صحيح فعلياً. الحل: إضافة حقل رقمي `timeToRevenueDays: number` في `ProjectIdea` والترتيب عليه.
- إغلاق النوافذ بمفتاح `Esc` + حصر التركيز (focus trap) + `role="dialog"` و `aria-modal` — حالياً `aria-label` واحد فقط في كامل المشروع.
- منع تمرير الصفحة خلف النافذة المنبثقة (`overflow: hidden` على `body`).
- تحقق من صحة المدخلات في `/api/generate-plan` + تحديد معدل الطلبات (rate limiting) لحماية مفتاح Gemini.

---

### المرحلة الثانية — تحويل المتصفح إلى منفّذ (قلب المنتج)

#### 2.1 خطة الإطلاق التفاعلية في 30 يوماً ⭐ الميزة الأهم
هذه هي الميزة التي تحوّل المنصة من "قائمة أفكار تُقرأ مرة" إلى "أداة تُستخدم يومياً".

- عند اختيار مشروع: زر **"ابدأ هذا المشروع"** ينشئ خطة تنفيذ شخصية.
- تحويل `actionSteps` + `marketingStrategy` الموجودين أصلاً في البيانات إلى **قائمة مهام مؤرّخة** موزعة على 4 أسابيع.
- شريط تقدم، شارات إنجاز، سلسلة أيام متتالية (streak).
- إشعارات متصفح تذكيرية (Notification API) للمهمة التالية.
- كل هذا ممكن بالكامل على `localStorage` دون حساب مستخدم.

#### 2.2 مقارنة المشاريع جنباً إلى جنب
- تحديد من 2 إلى 3 مشاريع ← جدول مقارنة: رأس المال، الربح، هامش الربح، سرعة العائد، الجهد اليومي، المخاطرة.
- توصية آلية: *"إن كان همّك سرعة العائد اختر أ، وإن كان هامش الربح اختر ب."*

#### 2.3 حاسبة أرباح متقدمة
الحاسبة الحالية تعطي رقماً واحداً ثابتاً، والواقع نطاق من الاحتمالات.

- **ثلاثة سيناريوهات**: متشائم / واقعي / متفائل تُحسب وتُعرض معاً.
- **رسم بياني لنقطة التعادل**: منحنى التكلفة مقابل الإيراد عبر 6 أشهر.
- **توقع التدفق النقدي** شهراً بشهر مع إعادة استثمار نسبة من الربح.
- **مؤشر الحساسية**: "لو انخفض سعر البيع 10% ينهار ربحك بنسبة 34%".
- **حاسبة الزكاة / الضريبة** حسب الدولة المختارة.
- تصدير النتائج إلى CSV.

#### 2.4 مستشار ذكي بالمحادثة
حالياً التفاعل مع الذكاء الاصطناعي يتم مرة واحدة فقط بلا متابعة.

- تحويل `AiPlanGenerator` إلى محادثة مستمرة: *"كيف أجد أول زبون؟"*، *"اكتب لي منشور إنستغرام لهذا المشروع"*.
- بث الإجابة تدريجياً (streaming) بدل انتظار 7 ثوانٍ صامتة.
- أدوات جاهزة بضغطة زر: توليد اسم تجاري، كتابة عرض سعر، صياغة رد على زبون متردد، خطة محتوى لأسبوع.
- الحفاظ على آلية الاحتياط الحالية (`buildSmartPlan`) فهي ممتازة ويجب ألا تُفقد.

#### 2.5 توسيع قاعدة الأفكار إلى 50+ فكرة
- تقسيم `projectsData.ts` (513 سطراً وسينمو) إلى ملفات حسب التصنيف.
- إضافة حقول: `riskLevel`, `competitionLevel`, `seasonality` (مشاريع رمضان، العودة للمدارس، الصيف).
- تصنيفات جديدة: مشاريع نسائية من المنزل، مشاريع للطلاب، مشاريع موسمية، مشاريع بدون تعامل مباشر مع الزبائن.
- تحميل تدريجي (lazy load) عند تجاوز 30 بطاقة.

---

### المرحلة الثالثة — نمو ومجتمع

#### 3.1 حسابات المستخدمين والمزامنة
مطلوبة عندما تتراكم بيانات حقيقية (خطط، تقدّم، مفضلة) يخسرها المستخدم بمسح المتصفح.
- تسجيل دخول بجوجل، تخزين في Firestore أو Supabase، مزامنة `localStorage` كطبقة أولى.

#### 3.2 قصص نجاح واقعية وتقييمات
- إمكانية إضافة تجربة حقيقية لكل مشروع: *"بدأت بـ 40$، أول ربح بعد 11 يوماً"*.
- تقييم واقعية الأرقام المعروضة من المستخدمين الفعليين — وهذا يصحّح التقديرات تدريجياً.

#### 3.3 أدلة عميقة لكل مشروع
- صفحة دليل مفصّل: من أين تشتري المواد، نماذج تسعير، قوالب رسائل للزبائن، أخطاء شائعة.
- هذه هي المادة المؤهلة لاحقاً لاشتراك مدفوع.

#### 3.4 نموذج تحقيق دخل
- **مجاني**: تصفح الأفكار، الحاسبة الأساسية، خطة ذكية واحدة شهرياً.
- **مدفوع**: خطط غير محدودة، المستشار بالمحادثة، تصدير PDF احترافي، الأدلة العميقة، متابعة الـ 30 يوماً.
- إعلانات محدودة أو روابط تسويق بالعمولة (أدوات، مواد، خدمات شحن).

#### 3.5 قياس ولوحة تحكم
- تتبع الأحداث: أي فكرة تُفتح، أي تصنيف يُستخدم، أين يتوقف المستخدم في الاختبار.
- لوحة تعرض الأفكار الأكثر طلباً — لتوجيه إضافة المحتوى بالبيانات لا بالتخمين.

---

### تحسينات مستمرة (عبر كل المراحل)

| المجال | المطلوب |
|---|---|
| الأداء | تقسيم الحزمة (`React.lazy`) للنوافذ المنبثقة، صور بصيغة WebP |
| PWA | العمل دون اتصال + التثبيت على الشاشة الرئيسية (مهم جداً لجمهور الهاتف) |
| إتاحة | تباين ألوان، تنقل بلوحة المفاتيح، قارئات الشاشة، دعم RTL كامل |
| جودة | اختبارات Vitest للحاسبة ومنطق التصفية + CI على GitHub Actions |
| توثيق | `README.md` (غير موجود حالياً) + خطوات التشغيل والنشر |

---

## 3. الترتيب المقترح للتنفيذ

```
الأسبوع 1-2   │ 1.5 إصلاحات  →  1.1 روابط ومشاركة  →  1.2 العملات
الأسبوع 3-4   │ 1.3 تصدير PDF  →  1.4 مكتبة الخطط
الأسبوع 5-8   │ 2.1 خطة الـ 30 يوماً  ⭐  →  2.3 الحاسبة المتقدمة
الأسبوع 9-12  │ 2.4 المستشار الذكي  →  2.2 المقارنة  →  2.5 توسيع الأفكار
ما بعد ذلك    │ المرحلة الثالثة حسب أرقام الاستخدام الفعلية
```

## 4. إن أردت البدء بميزة واحدة فقط

**خطة الإطلاق التفاعلية في 30 يوماً (2.1).**

السبب: البيانات اللازمة لها موجودة بالفعل في `actionSteps` و `marketingStrategy` داخل كل فكرة، فالتكلفة التقنية منخفضة نسبياً. وهي الميزة الوحيدة التي تعطي المستخدم سبباً للعودة غداً — وكل ميزة أخرى في هذه الوثيقة تصبح أكثر قيمة بوجودها.
